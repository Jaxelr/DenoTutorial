export function sayHello(thing: string) {
    console.log(`Hello, ${thing}!`);
  }

  // Use export keyword to make a function available outside of the module.
  export interface Foo {}
  export class Bar {}
  export const baz = "baz";